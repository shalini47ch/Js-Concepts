"use strict" //this ensures that all the code is in the new Js Version

// alert("hello") this wont run as we are using nodejs and this is possible in the browser

//another thing to keep in mind while coding is of readability






/*Datatypes(primitive)
1.number ->generally is in the range of 2**53
2.bigint->when we need to use much bigger values then we use bigint
3.string
4.Boolean->for true and false 
5.null->standalone value
6.undefined->you have declared a variable but havent assigned a value to it
7.symbol->unique(when we need to deal with unique values then we use symbol)

type of undefined is undefined 
type of null is object

Objects







*/

